# CI 最小依赖集
# 只覆盖当前测试与基础运行需要的依赖，避免引入未使用的原生编译包影响 CI 稳定性。

# 数据库 / ORM
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# 数据处理
pandas>=2.1.0
numpy>=1.26.0

# Web / API
fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.27.0

# 配置 / 日志
pyyaml>=6.0
loguru>=0.7.0
python-dotenv>=1.0.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
